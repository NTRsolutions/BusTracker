<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>

    <style name="MyButtonStyle1" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:drawableLeft">@drawable/ic_location_on_orange_24dp</item>
        <item name="android:background">@drawable/pickup_bkgrnd</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="MyButtonStyle2" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:drawableLeft">@drawable/ic_location_on_orang_24dp</item>
        <item name="android:background">@drawable/drop_bkgrnd</item>
        <item name="android:textColor">#fff</item>
    </style>

    <style name="MyTransparentDialog" parent="@android:style/Theme.Material.Dialog">
        <item name="android:windowBackground">@drawable/alert_backgrnd</item>
        <item name="android:background">@android:color/transparent</item>
    </style>


</resources>
